# TrajectoryPredictor

Projeto que visa criar predições de trajetórias de um objeto em um determinado ambiente bidimensional. Esse projeto foi feito para o curso de Machine Learning para Equações Diferenciais do PICME.

Para instalar e começar a usar o projeto:
- Crie uma pasta no seu dispositivo e baixe os arquivos ou use o comando `git clone https://github.com/VitinDenoyr/TrajectoryPredictor.git`
- Instale o **Python** (de preferência, na versão 3.12) e as bibliotecas **NumPy**, **PyTorch**, **MatPlotLib**, **ImageIo** e **PyGame** com o comando `python -m pip install -r requirements.txt`.
- Dentro da pasta criada, encontre o arquivo do notebook **main.ipynb**.
- Have Fun :)

# Legacy Version

Apenas os arquivos da versão inicial do projeto, concluída durante o PICME de machine learning para equações diferenciais. Podem apresentar bugs, principalmente pois algumas coisas podem funcionar diferente dependendo da versão do Python que está tentando ser executada. De preferência, use a 3.12 para a simulação.